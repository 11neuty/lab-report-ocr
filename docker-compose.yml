# Docker Compose for lab-report-ocr
#
# This file defines the multi-service architecture:
#   - frontend: Web application (React/Vite)
#   - backend:  API server (FastAPI/Python)
#
# Usage:
#   docker compose up -d
#   docker compose down
#
# Services are configured via the .env file.
# See .env.example for available environment variables.
