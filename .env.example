APP_NAME=lab-report-ocr
APP_VERSION=0.1.0
FRONTEND_PORT=5173
BACKEND_PORT=8000
UPLOAD_PATH=./uploads
OUTPUT_PATH=./output
# Browser must use localhost; Docker service names are not resolvable by browsers.
VITE_API_URL=http://localhost:8000
